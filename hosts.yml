all:
  children:
    dev:
      hosts:  
         xxx.xxx.xxx.xx:   # server
    stg:
      hosts:
        xxx.xxx.xxx.xx:   # server

    prod:
      hosts:
        xxx.xxx.xxx.xx:   # server

  vars: 
    ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
